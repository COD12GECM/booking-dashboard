<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <meta name="theme-color" content="#6366f1">
  <link rel="icon" type="image/png" href="https://cdn.shopify.com/s/files/1/0940/5274/5549/files/Untitled_design-100_550ad869-e845-4877-9844-37e6bd98b2a0.png?v=1766876822">
  <title>Super Admin Dashboard</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    
    :root {
      --neo-bg: #e0e5ec;
      --neo-shadow-dark: #a3b1c6;
      --neo-shadow-light: #ffffff;
      --neo-primary: #6366f1;
      --neo-success: #10b981;
      --neo-danger: #ef4444;
      --neo-warning: #f59e0b;
      --neo-text: #1e293b;
      --neo-text-muted: #64748b;
    }
    
    [data-theme="dark"] {
      --neo-bg: #1a1a2e;
      --neo-shadow-dark: #0f0f1a;
      --neo-shadow-light: #252542;
      --neo-text: #e2e8f0;
      --neo-text-muted: #94a3b8;
    }
    
    body {
      min-height: 100vh;
      background: var(--neo-bg);
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      padding: 40px 20px;
    }
    
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 20px;
      margin-bottom: 32px;
    }
    
    .stat-card {
      background: var(--neo-bg);
      border-radius: 16px;
      padding: 24px;
      text-align: center;
      box-shadow: 8px 8px 16px var(--neo-shadow-dark), -8px -8px 16px var(--neo-shadow-light);
    }
    
    .stat-value {
      font-size: 32px;
      font-weight: 700;
      color: var(--neo-primary);
    }
    
    .stat-label {
      font-size: 12px;
      color: var(--neo-text-muted);
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-top: 8px;
    }
    
    .theme-toggle {
      background: var(--neo-bg);
      border: none;
      padding: 10px;
      border-radius: 50%;
      cursor: pointer;
      box-shadow: 3px 3px 6px var(--neo-shadow-dark), -3px -3px 6px var(--neo-shadow-light);
    }
    .theme-toggle svg { width: 20px; height: 20px; stroke: var(--neo-text); }
    [data-theme="dark"] .sun-icon { display: block; }
    [data-theme="dark"] .moon-icon { display: none; }
    .sun-icon { display: none; }
    .moon-icon { display: block; }
    
    @media (max-width: 768px) {
      .stats-grid { grid-template-columns: repeat(2, 1fr); }
    }
    
    .container {
      max-width: 900px;
      margin: 0 auto;
    }
    
    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 40px;
    }
    
    .header-title {
      color: var(--neo-text);
      font-size: 28px;
      font-weight: 700;
    }
    
    .header-badge {
      background: linear-gradient(135deg, #6366f1, #8b5cf6);
      color: white;
      padding: 8px 20px;
      border-radius: 20px;
      font-size: 13px;
      font-weight: 600;
    }
    
    .logout-btn {
      background: var(--neo-bg);
      color: var(--neo-text-muted);
      padding: 12px 24px;
      border-radius: 12px;
      text-decoration: none;
      font-weight: 600;
      box-shadow: 4px 4px 8px var(--neo-shadow-dark), -4px -4px 8px var(--neo-shadow-light);
      transition: all 0.3s ease;
    }
    
    .logout-btn:hover {
      box-shadow: inset 4px 4px 8px var(--neo-shadow-dark), inset -4px -4px 8px var(--neo-shadow-light);
    }
    
    .card {
      background: var(--neo-bg);
      border-radius: 24px;
      padding: 32px;
      margin-bottom: 32px;
      box-shadow: 12px 12px 24px var(--neo-shadow-dark), -12px -12px 24px var(--neo-shadow-light);
    }
    
    .card-title {
      color: var(--neo-text);
      font-size: 18px;
      font-weight: 700;
      margin-bottom: 24px;
      text-transform: uppercase;
      letter-spacing: 2px;
    }
    
    .invite-form {
      display: grid;
      grid-template-columns: 1fr 1fr auto;
      gap: 16px;
      align-items: end;
    }
    
    .form-group {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }
    
    .form-label {
      color: var(--neo-text);
      font-size: 13px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    
    .form-input {
      padding: 14px 18px;
      background: var(--neo-bg);
      border: none;
      border-radius: 14px;
      font-size: 15px;
      color: var(--neo-text);
      box-shadow: inset 4px 4px 8px var(--neo-shadow-dark), inset -4px -4px 8px var(--neo-shadow-light);
    }
    
    .form-input:focus {
      outline: none;
    }
    
    .btn {
      padding: 14px 28px;
      border: none;
      border-radius: 14px;
      font-size: 15px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      color: #ffffff;
      box-shadow: 4px 4px 8px var(--neo-shadow-dark), -4px -4px 8px var(--neo-shadow-light);
    }
    
    .btn:hover {
      transform: translateY(-2px);
    }
    
    .success-msg {
      background: linear-gradient(135deg, #d1fae5, #a7f3d0);
      color: #065f46;
      padding: 16px 20px;
      border-radius: 12px;
      margin-bottom: 24px;
      font-size: 14px;
    }
    
    .owners-table {
      width: 100%;
      border-collapse: collapse;
    }
    
    .owners-table th {
      text-align: left;
      padding: 16px;
      color: var(--neo-text-muted);
      font-size: 12px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      border-bottom: 2px solid rgba(163, 177, 198, 0.3);
    }
    
    .owners-table td {
      padding: 16px;
      color: var(--neo-text);
      font-size: 15px;
      border-bottom: 1px solid rgba(163, 177, 198, 0.2);
    }
    
    .status-badge {
      display: inline-block;
      padding: 6px 14px;
      border-radius: 20px;
      font-size: 12px;
      font-weight: 600;
      text-transform: uppercase;
    }
    
    .status-pending {
      background: linear-gradient(135deg, #fef3c7, #fde68a);
      color: #92400e;
    }
    
    .status-active {
      background: linear-gradient(135deg, #d1fae5, #a7f3d0);
      color: #065f46;
    }
    
    .empty-state {
      text-align: center;
      padding: 40px;
      color: var(--neo-text-muted);
    }
    
    .action-btn {
      padding: 8px 16px;
      border: none;
      border-radius: 8px;
      font-size: 12px;
      font-weight: 600;
      cursor: pointer;
      margin-right: 8px;
      transition: all 0.2s ease;
    }
    
    .resend-btn {
      background: linear-gradient(135deg, #3b82f6, #2563eb);
      color: white;
    }
    
    .delete-btn {
      background: linear-gradient(135deg, #ef4444, #dc2626);
      color: white;
    }
    
    .action-btn:hover {
      transform: translateY(-1px);
      opacity: 0.9;
    }
    
    @media (max-width: 768px) {
      .invite-form {
        grid-template-columns: 1fr;
      }
      .header {
        flex-direction: column;
        gap: 16px;
        text-align: center;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div>
        <span class="header-badge">Super Admin</span>
        <h1 class="header-title" style="margin-top: 12px;">Owner Management</h1>
      </div>
      <div style="display: flex; gap: 12px; align-items: center;">
        <button type="button" class="theme-toggle" id="theme-toggle" title="Toggle dark mode">
          <svg class="moon-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>
          <svg class="sun-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/><line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/><line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/><line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/></svg>
        </button>
        <a href="/super-admin/logout" class="logout-btn">Logout</a>
      </div>
    </div>
    
    <% if (success) { %>
      <div class="success-msg"><%= success %></div>
    <% } %>
    
    <% if (error) { %>
      <div class="success-msg" style="background: linear-gradient(135deg, #fee2e2, #fecaca); color: #991b1b;"><%= error %></div>
    <% } %>
    
    <!-- Stats Grid -->
    <div class="stats-grid">
      <div class="stat-card">
        <div class="stat-value"><%= stats?.totalOwners || 0 %></div>
        <div class="stat-label">Total Owners</div>
      </div>
      <div class="stat-card">
        <div class="stat-value" style="color: var(--neo-success);"><%= stats?.activeOwners || 0 %></div>
        <div class="stat-label">Active</div>
      </div>
      <div class="stat-card">
        <div class="stat-value" style="color: var(--neo-warning);"><%= stats?.pendingOwners || 0 %></div>
        <div class="stat-label">Pending</div>
      </div>
      <div class="stat-card">
        <div class="stat-value"><%= stats?.totalBookings || 0 %></div>
        <div class="stat-label">Total Bookings</div>
      </div>
    </div>
    
    <div class="card">
      <h2 class="card-title">Invite New Owner</h2>
      <form method="POST" action="/super-admin/invite" class="invite-form">
        <div class="form-group">
          <label class="form-label">Email Address</label>
          <input type="email" name="email" class="form-input" placeholder="owner@clinic.com" required>
        </div>
        <div class="form-group">
          <label class="form-label">Clinic Name (Optional)</label>
          <input type="text" name="clinicName" class="form-input" placeholder="Aesthetica Clinic">
        </div>
        <button type="submit" class="btn">Send Invitation</button>
      </form>
    </div>
    
    <div class="card">
      <h2 class="card-title">All Owners (<%= owners.length %>)</h2>
      
      <% if (owners.length === 0) { %>
        <div class="empty-state">
          <p>No owners yet. Send your first invitation above.</p>
        </div>
      <% } else { %>
        <table class="owners-table">
          <thead>
            <tr>
              <th>Email</th>
              <th>Clinic</th>
              <th>Status</th>
              <th>Bookings</th>
              <th>This Month</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% owners.forEach(owner => { %>
              <tr>
                <td><%= owner.email %></td>
                <td><%= owner.clinicName || '-' %></td>
                <td>
                  <span class="status-badge status-<%= owner.status %>">
                    <%= owner.status %>
                  </span>
                </td>
                <td style="font-weight: 600;"><%= owner.totalBookings || 0 %></td>
                <td style="color: var(--neo-primary); font-weight: 600;"><%= owner.monthlyBookings || 0 %></td>
                <td>
                  <% if (owner.status === 'pending') { %>
                    <form method="POST" action="/super-admin/resend/<%= owner._id %>" style="display: inline;">
                      <button type="submit" class="action-btn resend-btn">Resend</button>
                    </form>
                  <% } %>
                  <% if (owner.status === 'active') { %>
                    <a href="/super-admin/ghost/<%= owner._id %>" class="action-btn" style="background: linear-gradient(135deg, #8b5cf6, #7c3aed); color: white; text-decoration: none; display: inline-block;">Ghost</a>
                  <% } %>
                  <form method="POST" action="/super-admin/delete/<%= owner._id %>" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this owner?');">
                    <button type="submit" class="action-btn delete-btn">Delete</button>
                  </form>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } %>
    </div>
  </div>
  
  <script>
    // Dark Mode Toggle
    const themeToggle = document.getElementById('theme-toggle');
    const savedTheme = localStorage.getItem('theme') || 'light';
    document.documentElement.setAttribute('data-theme', savedTheme);
    
    themeToggle.addEventListener('click', () => {
      const currentTheme = document.documentElement.getAttribute('data-theme');
      const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
      document.documentElement.setAttribute('data-theme', newTheme);
      localStorage.setItem('theme', newTheme);
    });
  </script>
</body>
</html>
